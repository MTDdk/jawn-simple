rootProject.name = 'jawn-simple'

// TODO to be removed when merged to master
['jawn-server-undertow', 'jawn-templates-stringtemplate', 'jawn-core'].each {
  include it
  project(":$it").projectDir = file("../jawn/$it")
}
